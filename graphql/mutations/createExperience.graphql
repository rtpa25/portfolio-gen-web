mutation CreateExperience($input: CreateExperienceInput!) {
    createExperience(input: $input) {
        experience {
            ...ExperienceData
        }
        errors {
            ...FieldError
        }
    }
}
