mutation CreateSocialLink($input: CreateSocialLinksInput!) {
    createSocialLink(input: $input) {
        socialLink {
            _id
            link
            name
            user
        }
        errors {
            field
            message
        }
    }
}
